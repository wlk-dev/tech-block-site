<section class="container px-5 py-6">
    <div class="card p-4 m-2">
        <h5 class="">{{title}}</h5>
        <p class="card-body lead">{{content}}</p>
        <p><small class="p-1 text-white bg-info">By {{user.username}}</small><small class="p-1 text-white bg-dark">{{format_date createdAt}}</small></p>
        <button id="comment">Comment</button>
    </div>
</section>
{{#each comments as |comment|}}
{{> comment-bit}}
{{/each}}